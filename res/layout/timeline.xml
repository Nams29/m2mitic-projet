<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/timeline"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/bg_frise" >

    <!-- Permet de positionner les groupes de photos par rapport à la flèche. -->

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_centerVertical="true"
        android:background="#FFF"
        android:orientation="horizontal" >
    </LinearLayout>

    <!-- Création de la flèche -->

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:orientation="horizontal" >

        <!-- Partie gauche -->

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="20dp"
            android:background="@drawable/left_arrow"
            android:contentDescription="@string/timeline_arrowLeftPart" />

        <!-- Partie droite -->

        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginRight="20dp"
            android:background="@drawable/right_arrow"
            android:contentDescription="@string/timeline_arrowRightPart" />
    </LinearLayout>

</RelativeLayout>